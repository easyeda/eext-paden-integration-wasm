[简体中文](./README.md) | [English](./README.en.md) | [繁體中文](./README.zh-Hant.md) | [日本語](./README.ja.md) | [Русский](#)

# PADEN Симуляция

Расширение JLCEDA & EasyEDA Pro — извлечение данных PCB и выполнение FEM-анализа PDN (сети распределения питания)

> **Версия**: 1.0.7 | **Категория**: PCB | **Ключевые слова**: PDN, Power Analysis, Simulation, 仿真, PI

## Возможности

- Извлечение трасс, переходных отверстий, контактных площадок и медных заливок из EasyEDA
- Пользовательская конфигурация шин питания (источники напряжения, токовые нагрузки, толщина меди слоёв)
- Встроенный аналитический движок Go/WebAssembly — не требуется отдельный бэкенд или сервис
- Парсинг Gerber через tracespace, булевы операции/офсеты многоугольников через Clipper2-WASM, триангуляция через earcut
- WebGL-визуализация распределения напряжения и тепловой карты плотности мощности
- Поддержка анализа нескольких шин питания (1 комбинированное решение + N индивидуальных решений)

## Архитектура

```
EasyEDA PCB
    │
    ▼
┌─────────────┐     ┌──────────────┐     ┌─────────────────────────┐
│  extract.ts │────▶│  convert.ts  │────▶│  ui/wasm-host.html      │
│  Извлечение │     │  Конвертация │     │  Go/WASM хост IFrame    │
└─────────────┘     └──────────────┘     └─────────────────────────┘
                                                   │
                                                   ▼
                                         ┌─────────────────────────┐
                                         │  go-service/internal/   │
                                         │  pipeline               │
                                         │  ├ Парсинг Gerber       │
                                         │  ├ Геометрия            │
                                         │  │  (clipper2)          │
                                         │  ├ Триангуляция         │
                                         │  └ FEM-решатель         │
                                         └───────────┬─────────────┘
                                                     │
                                                     ▼
┌─────────────┐     ┌──────────────┐     ┌─────────────────────────┐
│  display.ts │◀────│ wasmClient.ts│◀────│  JSON result            │
│  Отображение│     │  WASM comms  │     └─────────────────────────┘
└──────┬──────┘     └──────────────┘
       │
       ▼
┌─────────────┐
│ results.html│  WebGL визуализация
└─────────────┘
```

## Инструкция по использованию

### 1. Установите расширение в JLCEDA Pro (3.2+)
После установки выполните настройку

![Нажмите «Настроить»](./images/img-1.png)

![Отметьте «Разрешить внешнее взаимодействие»](./images/img-2.png)

### 2. Расширение доступно в редакторе PCB в рамках инженерного проекта

### 3. Через верхнее меню: Дополнительно → PADEN Симуляция → Запустить PDN анализ

### 4. Выберите параметры анализа и нажмите «Начать анализ»

![Извлечение данных PCB](./images/img-3.png)

![Извлечение данных PCB](./images/img-4.png)

## Быстрый старт

### 1. Установка зависимостей frontend

```shell
npm install
```

### 2. Компиляция расширения

```shell
npm run compile
```

### 3. Сборка WASM-аналитического движка

```shell
npm run build:wasm-host-bridge
npm run build:wasm
```

Полная релизная сборка (TypeScript + WASM bridge + Go WASM + копирование ресурсов + `.eext` упаковка):

```shell
npm run build
```

Сборка WASM для разработки (сохраняет символы, файл больше):

```shell
npm run build:wasm:dev
```

### 4. Установка в EasyEDA

1. Откройте JLCEDA Pro, войдите в редактор PCB
2. Установите пакет расширения
3. Выберите **PDN анализ → Запустить PDN анализ...** в меню

> Внешний бэкенд-сервис не требуется; весь анализ выполняется внутри WASM-рантайма EasyEDA.

## Структура проекта

```
├── src/                    # TypeScript frontend
│   ├── index.ts            # Главная точка входа, организация анализа
│   ├── extract.ts          # Извлечение данных PCB (трассы, VIA, площадки, медь)
│   ├── convert.ts          # Конвертация данных и построение конфигурации
│   ├── wasmClient.ts       # Загрузка Go/WASM хоста IFrame и коммуникация через MessageBus
│   ├── display.ts          # Отображение результатов (IFrame + Storage + MessageBus)
│   └── types.ts            # Определения типов
├── ui/                     # HTML-файлы диалогов
│   ├── config.html         # UI конфигурации шин питания
│   ├── results.html        # WebGL визуализация результатов
│   ├── analyzing.html      # UI прогресса анализа
│   └── wasm-host.html      # Скрытый Go/WASM хост IFrame
├── go-service/             # Go/WebAssembly бэкенд
│   ├── main_wasm.go        # Вход WASM, предоставляет JS API analyzeGerber
│   ├── internal/pipeline/  # Полный аналитический pipeline
│   ├── internal/problem/   # Определение задачи (слои, сети, переходные отверстия)
│   ├── internal/solver/    # FEM-решатель и разреженные матрицы
│   ├── internal/mesh/      # Интерфейсы сетки и триангуляции
│   ├── internal/geometry/  # Парсинг Gerber, Clipper2, мост earcut
│   └── internal/wasmapi/   # Сериализация результатов
├── config/                 # Конфигурация esbuild
├── scripts/                # build:wasm / build:wasm-host-bridge / copy-wasm-assets
├── build/                  # Скрипт упаковки `.eext`
├── dist/                   # Вывод сборки (index.js, paden.wasm, wasm_exec.js и т.д.)
└── extension.json          # Манифест расширения
```

## Рабочий процесс

1. **Извлечение данных** — Извлечение трасс, переходных отверстий, площадок и медных областей из текущего PCB
2. **Конфигурация анализа** — Выбор цепей питания, настройка источников напряжения и токовых нагрузок
3. **Парсинг Gerber** — Движок Go/WASM разбирает геометрию медных слоёв из Gerber ZIP через tracespace
4. **Геометрическая обработка** — Булевы операции и офсеты через Clipper2-WASM, триангуляция через earcut
5. **FEM-решение** — Построение матрицы Лапласа и решение распределения напряжения
6. **Визуализация** — Тепловая карта напряжения WebGL с переключением слоёв, отображением рёбер сетки и маркерами VIA

## Технологии

**Frontend**: TypeScript, esbuild, WebGL

**Backend**: Go 1.26+, WebAssembly, `syscall/js`

**Геометрия/сетка**: `@tracespace/parser`, `@tracespace/plotter`, `clipper2-wasm`, `earcut`

**Зависимости**: `@jlceda/pro-api-types`, `@tracespace/parser`, `@tracespace/plotter`, `clipper2-wasm`, `earcut`

## Лицензия

Это расширение использует лицензию [Apache License 2.0](https://choosealicense.com/licenses/apache-2.0/).

---

## Ссылки

- **Главная страница**: https://github.com/easyeda/eext-paden-integration
- **Трекер проблем**: https://github.com/easyeda/eext-paden-integration/issues
